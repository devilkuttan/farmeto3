<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup Completed Successfully</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      overflow: hidden; /* Hide overflow to prevent horizontal scrolling */
      margin: 0;
      padding: 0;
    }
    .container {
      margin-top: 100px;
      text-align: center;
    }
    .alert {
      border-radius: 15px;
      background-color: #f1f8e9;
      border-color: #c8e6c9;
      color: #2e7d32;
      max-width: 400px;
      margin: 0 auto;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
    }
    .alert h4 {
      color: #1b5e20;
      font-weight: bold;
      margin-bottom: 15px;
    }
    .alert hr {
      border-top-color: #c8e6c9;
    }
    .alert p {
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 20px;
    }
    .btn {
      background-color: #4caf50;
      border: none;
      color: white;
      padding: 10px 20px;
      text-align: center;
      text-decoration: none;
      display: inline-block;
      font-size: 16px;
      border-radius: 5px;
      transition: background-color 0.3s;
    }
    .btn:hover {
      background-color: #45a049;
    }
    
    /* Popper styles */
    .popper {
      position: absolute;
      width: 20px;
      height: 20px;
      border-radius: 50%; /* Makes the popper shape circular */
      animation: falling 5s linear infinite;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    
    /* Animation keyframes */
    @keyframes falling {
      0% {
        transform: translateY(-100px) rotate(0deg);
        opacity: 0; /* Start with opacity 0 */
      }
      100% {
        transform: translateY(100vh) rotate(360deg);
        opacity: 1; /* End with opacity 1 */
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="alert alert-success" role="alert">
      <h4 class="alert-heading">Signup Completed Successfully</h4>
      <p>Congratulations! You have successfully signed up.</p>
      <hr>
      <p class="mb-4">We will verify your account as soon as possible.</p>
      <a href="/" class="btn btn-lg btn-success">Home</a>
    </div>
  </div>
  <!-- Popper Elements -->
  <script>
    function getRandomColor() {
      var letters = '0123456789ABCDEF';
      var color = '#';
      for (var i = 0; i < 6; i++) {
        color += letters[Math.floor(Math.random() * 16)];
      }
      return color;
    }
    for (var i = 0; i < 50; i++) { // Increased to 50 poppers
      var popper = document.createElement('div');
      popper.className = 'popper';
      popper.style.left = Math.random() * 100 + '%';
      popper.style.animationDuration = (Math.random() * 5 + 4) + 's';
      popper.style.backgroundColor = getRandomColor();
      document.body.appendChild(popper);
    }
  </script>
</body>
</html>
